# 观察者模式
> 行为型设计模式

观察者模式 亦称： 事件订阅者、监听者、Event-Subscriber、Listener、Observer ## 介绍 观察者模式 是一种行为设计模式， 允许你定义一种订阅机制， 可在对象事件发生时通知多个 “观察” 该对象的其他对象。 发布订阅模式大概是前端同学最熟悉的设计模式之一了，常见的事件监听 addEventListener ，各种属性方法 onload 、 onchange ， vue 响应式数据，组件通信 redux 、 EventEmitter 等。 ## 结构 发布者 （Publisher） 会向其他对象发送值得关注的事件。 事件会在发布者自身状态改变或执行特定行为后发生。 发布者中包含一个允许新订阅者加入和当前订阅者离开列表的订阅构架。 当新事件发生时， 发送者会遍历订阅列表并调用每个订阅者对象的通知方法。 该方法是在订阅者接口中声明的。 订阅者 （Subscriber） 接口声明了通知接口。 在绝大多数情况下， 该接口仅包含一个 update 更新 方法。 该方法可以拥有多个参数， 使发布者能在更新时传递事件的详细信息。 具体订阅者 （Concrete Subscribers） 可以执行一些操作来回应发布者的通知。 所有具体订阅者类都实现了同样的接口， 因此发布者不需要与具体类相耦合。 订阅者通常需要一些上下文信息来正确地处理更新。 因此， 发布者通常会将一些上下文数据作为通知方法的参数进行传递。 发布者也可将自身作为参数进行传递， 使订阅者直接获取所需的数据。 客户端 （Client） 会分别创建发布者和订阅者对象， 然后为订阅者注册发布者更新。 ## 与其他模式的关系 责任链模式 、 命令模式 、 中介者模式 和 观察者模式 用于处理请求发送者和接收者之间的不同连接方式： 责任链 按照顺序将请求动态传递给一系列的潜在接收者， 直至其中一名接收者对请求进行处理。 命令 在发送者和请求者之间建立单向连接。 中介者 清除了发送者和请求者之间的直接连接， 强制它们通过一个中介对象进行间接沟通。 观察者 允许接收者动态地订阅或取消接收请求。 和 中介者模式 的区别 中介者模式 ：通过中介者协调多个对象之间的复杂交互，减少对象之间的直接依赖。 观察者模式 ：通过发布-订阅机制，一个对象状态变化时通知多个观察者，解耦事件源与事件处理者。 它们的区别在于 通信的方向 和 对象的依赖结构 ：中介者模式是通过一个中心进行双向通信，观察者模式则是一对多的单向通知。 ## 优缺点 ✔️ 开闭原则。 你无需修改发布者代码就能引入新的订阅者类 （如果是发布者接口则可轻松引入发布者类）。 ✔️ 你可以在运行时建立对象之间的联系。 ## 实战示例 EventEmitter class EventEmitter { // 使用 Map&lt;Events, Set&lt;ICallback&gt;&gt; 来存储事件监听器 listeners = new Map() /** * 添加事件监听 * @param event 事件 * @param callback 回调函数 */ on(event, callback) { if (!listeners.has(event)) { listeners.set(event, new Set()) } listeners.get(event)?.add(callback) } /** * 移除事件监听 * @param event 事件 * @param callback 回调函数 */ off(event, callback) { if (listeners.has(event)) { const listenerSet = listeners.get(event) listenerSet?.delete(callback) if (listenerSet?.size === 0) { listeners.delete(event) } } } /** * 触发事件 * @param event 事件 * @param params 事件参数 * @param emitDelayEvent 是否将事件缓存用于订阅时触发 */ emit(event, params, emitDelayEvent = false) { if (listeners.has(event)) { listeners.get(event)?.forEach(f =&gt; f(params)) } } } export default EventEmitter 改善发布订阅模式 单次监听事件 /** * 添加一次性事件监听 * @param event 事件 * @param callback 回调函数 */ once(event, callback) { // 创建一个包装函数，调用后自动移除 const onceCallback = (params) =&gt; { // 执行回调 callback(params) // 事件触发后自动移除监听 this.off(event, onceCallback) } this.on(event, onceCallback) } 事件缓存机制 class EventEmitter { // 使用 Map&lt;Events, any[]&gt; 来存储延迟触发的参数 delayEmitParams = new Map() // 使用 Map&lt;Events, Set&lt;ICallback&gt;&gt; 来存储事件监听器 listeners = new Map() /** * 添加事件监听 * @param event 事件 * @param callback 回调函数 */ on(event, callback) { if (!listeners.has(event)) { listeners.set(event, new Set()) } listeners.get(event)?.add(callback) const eventDelayEmitParams = delayEmitParams.get(event) if (eventDelayEmitParams) { eventDelayEmitParams.forEach(params =&gt; { callback(params) }) delayEmitParams.delete(event) } } /** * 移除事件监听 * @param event 事件 * @param callback 回调函数 */ off(event, callback) { if (listeners.has(event)) { const listenerSet = listeners.get(event) listenerSet?.delete(callback) if (listenerSet?.size === 0) { listeners.delete(event) } } } /** * 触发事件 * @param event 事件 * @param params 事件参数 * @param emitDelayEvent 是否将事件缓存用于订阅时触发 */ emit(event, params, emitDelayEvent = false) { if (listeners.has(event)) { listeners.get(event)?.forEach(f =&gt; f(params)) } // 暂时没有监听回调时先缓存 if (emitDelayEvent) { if (!delayEmitParams.has(event)) { delayEmitParams.set(event, []) } delayEmitParams.get(event)?.push(params) } } } export default EventEmitter 增加命名空间 class EventEmitter { // 使用 Map&lt;namespace, Map&lt;eventName, Set&lt;callback&gt;&gt;&gt; 来存储监听器 listeners = new Map() /** * 添加事件监听 * @param namespace 命名空间 * @param event 事件名 * @param callback 回调函数 */ on(namespace, event, callback) { if (!this.listeners.has(namespace)) { this.listeners.set(namespace, new Map()) } const eventMap = this.listeners.get(namespace) if (!eventMap.has(event)) { eventMap.set(event, new Set()) } eventMap.get(event)?.add(callback) } /** * 移除事件监听 * @param namespace 命名空间 * @param event 事件名 * @param callback 回调函数 */ off(namespace, event, callback) { if (this.listeners.has(namespace)) { const eventMap = this.listeners.get(namespace) const listenersSet = eventMap.get(event) listenersSet?.delete(callback) if (listenersSet?.size === 0) { eventMap.delete(event) } if (eventMap?.size === 0) { this.listeners.delete(namespace) } } } /** * 触发事件 * @param namespace 命名空间 * @param event 事件名 * @param params 事件参数 */ emit(namespace, event, params) { if (this.listeners.has(namespace)) { this.listeners .get(namespace) ?.get(event) ?.forEach(callback =&gt; callback(params)) } } /** * 移除整个命名空间下的所有事件监听 * @param namespace 命名空间 */ offNamespace(namespace) { if (this.listeners.has(namespace)) { this.listeners.delete(namespace) } } } export default EventEmitter
